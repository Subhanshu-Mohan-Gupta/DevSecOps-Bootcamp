Initial set up Kyverno through helm
  helm repo add kyverno https://kyverno.github.io/kyverno/
  helm repo update
  helm install kyverno kyverno/kyverno -n kyverno --create-namespace


Write two policy files in yaml.

Created two pods god and bad for policy testing.

Checked Kyverno Reports
  kubectl get policyreports -A


Created github action pipeline for the task.

Problems faced
Kyverno admission controller cannot be run on minikube for limited resources.Done on EKS.
Reports of all the namespaces were generated without sepration so update the kyverno set up to generate separate report for each namespace.
  
  
